/* Market codes as the short names a brand actually uses.
 *
 * This file used to carry the landing page's photography: a rotating
 * hero cover, a grid of creator portraits, a framed post beside the
 * guarantee, and two product crops. All of it is gone with the sections
 * that held it. A grid of faces made the page read as a marketplace for
 * influencers, which HeyMoon is not: it sells guaranteed sales, and
 * creators are how the guarantee is delivered, not the thing on offer.
 *
 * The one human element left is the three real avatars inside the plan
 * mock, which is exactly where the product itself puts them.
 */
export const SHORT_MARKET: Record<string, string> = {
  AE: "UAE", SA: "KSA", KW: "Kuwait", QA: "Qatar", BH: "Bahrain", OM: "Oman",
};
